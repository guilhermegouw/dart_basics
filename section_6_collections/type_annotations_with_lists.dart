main() {
  /* As we prefer to declared variables in dart using var, const and final 
  We can still infer the types inside the list when declaring a variable using
  var, like this */
  var cities = <String>['London', 'Paris', 'Moscow'];
}
