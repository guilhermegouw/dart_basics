/*
Using only variable defined like this:

String title = 'Dart course';

Write a rpgram that uses "title" to produce the following output:

Dart course
DART COURSE
dart course
*/

main() {
  String title = 'Dart course';
  print(title);
  print(title.toUpperCase());
  print(title.toLowerCase());
}
